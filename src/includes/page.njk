<!DOCTYPE html>
<html class="page" lang="{{ lang }}">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="description" content="{{ description }}">
	<title>SVG vs PNG</title>
	<link rel="stylesheet" href="../styles/index.css">
	<link rel="icon" href="../favicon.ico" sizes="32x32">
	<link rel="apple-touch-icon" href="../icons/180.png">
	<link rel="manifest" href="manifest.json">
</head>
<body class="page__body">
	<div class="info">
		<button
			class="info__button"
			type="button"
			popovertarget="info"
		>I</button>
		<div class="info__description" id="info" popover>
			{{ content | safe }}
			<button
				class="info__button"
				data-type="close"
				type="button"
				popovertarget="info"
			>X</button>
		</div>
	</div>
	<nav class="languages">
		{%- for page in collections.pages %}
		<a
			href="/{{ page.data.lang }}/"
			hreflang="{{ page.data.lang }}"
			{{ ('aria-current="page"' if lang == page.data.lang) | safe }}
		>
			{{ page.data.lang | upper }}
		</a>
		{%- endfor %}
	</nav>
	{% svg lang %}
</body>
</html>
